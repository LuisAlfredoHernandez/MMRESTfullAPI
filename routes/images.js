const { Router } = require('express');
const { getImages } = require('../controllers/images');
const router = Router();


router.get('/', getImages)

router.post('')

router.delete('')

router.put('');
