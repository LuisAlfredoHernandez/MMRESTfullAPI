const { Router } = require('express');
const {  } = require('../controllers/');
const router = Router();


router.get('/')

// router.post('/')

// router.delete('/')

// router.put('/');

module.exports = router